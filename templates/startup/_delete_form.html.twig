<form method="post" action="{{ path('app_startup_delete', {'id': startup.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ startup.id) }}">
    <button class="btn">Delete</button>
</form>
